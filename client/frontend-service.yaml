apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort
  selector:
    app: response-tool-frontend
  ports:
    - port: 80
      targetPort: 5173
      nodePort: 30007
